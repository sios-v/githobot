let function{
	class_name.prototype.method_name = function(first_argument) {
		// body...
	class_name: first_argument class_name
	};
	#!/usr/bin/env 
	env
	for (var i = 0; i < Things.length; i++) {
		Things[i]
	}
}